/**
 * Blog card cover image. This is just the shared {@link WarmImage} primitive
 * (fill + blur-up + warm-promotion) — re-exported under the blog-specific name
 * the index grid + related-posts strip import. See MEDIA_LOADING_PLAYBOOK.md.
 */
export { WarmImage as BlogCardImage } from "@/components/ui/WarmImage";
export { WarmImage as default } from "@/components/ui/WarmImage";
